# Temporary Supabase end-to-end verification — 2026-09-21

Scope: synthetic data only in `ageful-crm-verification-20260921`. Production was not changed.

## Verified

- Supabase Auth login and the configured single-owner access path.
- Anonymous reads of `customers` are denied.
- Ownership transfer A to B is atomic; the prior paid invoice remains assigned to A and future invoice/direct-debit units move to B.
- The transfer event keeps full project/contract before-and-after snapshots and the operator reason.
- Invoice issue, frozen line items/amount, payment confirmation, and reasoned correction.
- Direct-debit failure changes only that occurrence to an invoice for the same recipient; invoice issue and payment then complete normally.
- Manual direct-debit plan creation, successful receipt, and reasoned amount correction.
- An injected event-write failure leaves the direct-debit occurrence planned, pending, unfrozen, and with no receipt event; retrying the same occurrence with a normal date succeeds.
- Final synthetic state: one transfer, five billing units, thirteen audit events, no incomplete operation, and no event from the rejected write.

## Runtime permission correction found by this rehearsal

The private write helpers must run with the runtime function owner's privileges because the ledger/audit tables intentionally have no authenticated write grants. Authenticated users receive owner-scoped `SELECT` only on `billing_operations` and `billing_unit_events`; writes remain available only through `billing_runtime_write`.

## Verification commands

- `npm run test:billing-runtime` — passed.
- `npm run build` — passed (existing bundle-size warning only).

The conditional failure triggers exist only in the temporary staging project and match staging-specific values/project id. They are not part of the production migration files.

## 2026-09-22 latest billing-detail UI pass

Using the current normal application screen and the same synthetic project, the following operations were saved through the authenticated runtime and then visibly reloaded from Supabase:

- Create an invoice plan for a future maintenance period.
- Change its scheduled date and planned amount.
- Issue the invoice with frozen line items and amount.
- Record payment, then correct the payment date with a required reason.
- Create one explicit direct-debit plan.
- Record a successful debit with actual line items and amount.

The invoice finished as paid at 123,456 yen and the direct-debit occurrence as paid at 12,400 yen. Both retained the explicitly selected recipient. No production data was used or changed.

This pass found and fixed one real client-side problem: selected future-schedule items contained an `undefined` property and were rejected by strict JSON serialization before any database write. The payload now copies only defined serializable fields, with a regression test.
