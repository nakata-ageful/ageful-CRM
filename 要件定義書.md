# ageful-CRM 要件定義書

**太陽光発電所 顧客・保守・請求管理システム**

| 項目 | 内容 |
|---|---|
| システム名 | ageful-CRM |
| 種別 | Webアプリケーション |
| 文書バージョン | 1.0 |
| 作成日 | 2026-07-31 |

---

## 目次

1. システム概要
2. 用語定義
3. システム構成・技術要件
4. 機能要件
5. 請求計算仕様
6. データ要件
7. 非機能要件
8. 制約・既知の留意事項

---

## 1. システム概要

### 1.1 目的

本システムは、太陽光発電所の運営・保守事業における顧客情報、発電所（案件）情報、保守契約、保守対応、および請求業務を一元管理することを目的とした業務用Webアプリケーションである。営業段階の見込み案件から、成約後の顧客・発電所・契約情報、日々の保守対応記録、月次・年次の請求管理までを、単一のシステム上で継続的に扱えることを狙いとする。

### 1.2 対象ユーザー

- 発電所の保守・管理業務を行う社内担当者（管理者）
- 請求・入金管理を行う事務担当者
- 営業・案件進捗を管理する担当者

利用形態は社内の少人数運用を前提とし、PC（ブラウザ）を主とするが、画面分割・スマートフォンでの閲覧・操作にも対応する。

### 1.3 提供価値

- 顧客・発電所・契約・保守・請求という関連情報の分散を解消し、発電所単位で横断的に参照できる
- 請求金額を保守契約の各費目から自動算出し、口座振替・請求書の両方式に対応する
- 未入金・請求予定・保守対応中などの「対応すべき事項」をダッシュボードで即時に把握できる

---

## 2. 用語定義

| 用語 | 意味 |
|---|---|
| 見込み（Prospect） | 成約前の営業案件。申請・契約の進捗タスクを管理する。成約すると顧客・発電所・契約が一括生成される。 |
| 発電所 / 案件（Project） | 太陽光発電所1基を表す単位。基本情報・設備情報・契約情報・保守情報を持つ。 |
| 顧客（Customer） | 発電所の所有者。個人または法人。1顧客が複数発電所を持ちうる。 |
| 契約（Contract） | 1発電所に紐づく保守・請求条件。金額・請求方法・保守プラン等を保持する。 |
| 請求記録（年次記録 / Annual Record） | 年度単位の請求・入金実績。年複数回請求は1記録内に「回ごとの入金」を保持する。 |
| 保守対応 | 故障・問い合わせなど、随時発生する保守イベントの対応記録。 |
| 定期保守 | 点検・除草・経産省定期報告など、計画的に実施する保守の記録。 |
| 受託 / 委託 | 受託会社＝当社が保守を請け負う先。保守委託先＝当社が保守を外部へ委託する先。 |

---

## 3. システム構成・技術要件

### 3.1 技術スタック

| 層 | 採用技術 | 備考 |
|---|---|---|
| フロントエンド | React 19 / TypeScript / Vite | 単一ページアプリ（SPA）。外部UIフレームワーク不使用、独自CSS。 |
| バックエンド / DB | Supabase（PostgreSQL） | DBアクセスはSupabaseクライアント経由。サーバーサイドコードは持たない。 |
| 認証 | Supabase Auth（メール / パスワード） | ログイン必須。サインイン・サインアップに対応。 |
| ホスティング | Vercel | GitHubリポジトリ（main）への push で自動デプロイ。 |

### 3.2 画面構成

左サイドバーのナビゲーションから、以下の主要画面へ遷移する。一覧画面から各詳細画面へドリルダウンする構成をとる。

| 画面 | 役割 |
|---|---|
| ダッシュボード | 顧客数・案件数・対応中の保守・未入金・請求予定など、対応すべき事項の集約表示。 |
| 見込み管理 | 営業案件の進捗（申請・契約タスク）管理。成約処理。 |
| 発電所 | 発電所一覧・詳細（基本 / 設備 / 契約 / 保守 / 保守対応 / 請求 / 請求詳細）。 |
| 顧客 | 顧客一覧・詳細。1顧客の保有発電所を横断表示。 |
| 保守対応 | 保守対応・定期保守・受託情報・委託情報の4タブ一覧。 |
| 請求 | 請求予定・未入金・入金済・口座振替の運用画面。 |
| データのエクスポート | CSVインポート、データのCSV / JSONエクスポート、バックアップ復元。 |

---

## 4. 機能要件

### 4.1 認証

- メールアドレスとパスワードによるログインを必須とする。未認証ではアプリを利用できない。
- 新規ユーザー登録（サインアップ）にも対応する。

### 4.2 ダッシュボード

- KPI（顧客数・案件数・対応中の保守・未入金件数と合計金額・口座振替の要確認件数）を表示する。
- 「対応中の保守」「未入金アラート」「請求予定」の一覧を表示し、各行から詳細へ遷移できる。
- 請求に関する集計（未入金・請求予定・振替要確認）は請求画面と**完全に同一のロジック**で算出し、両画面で数値が一致する。

### 4.3 見込み管理

- 見込み案件の登録・編集・削除。申請状況・契約状況、およびタスク／サブタスクの進捗を管理する。
- 見込み登録時に、顧客・発電所・空契約を一括生成する。
- 見込みと発電所は**双方向同期**する。見込み側の編集は発電所・顧客・契約へ、発電所・顧客・契約側の編集は成約元の見込みへ反映される。

### 4.4 発電所管理

発電所詳細はタブ構成をとり、保守関連・請求関連でグループ化して配置する。並び順は「基本情報 / 設備情報 / 契約情報 / 保守情報 / 保守対応 / 請求情報 / 請求詳細 / その他」。

| タブ | 管理項目 |
|---|---|
| 基本情報 | 案件番号・発電所名・住所・座標、経産省（設備ID・認定日）、電力会社（受給地点番号・FIT・FIT満了年月・検針日 ほか） |
| 設備情報 | パネル・パワコン（KW・メーカー・型式・台数）、遠隔監視（メーカー・ID・4G対応） |
| 契約情報 | 設備契約・土地契約（契約者・販売会社・金額・各契約日・紹介者・所有権移転日 ほか） |
| 保守情報 | 保守契約（受託会社・年次保守料・保守開始日）、保守内容（点検 / 除草 / 駆けつけプラン・保険料 ほか）、委託契約 |
| 保守対応 | 定期保守（点検 / 除草 / 経産省定期報告）と保守対応記録。見出しに保守プラン・保守開始日を表示。 |
| 請求情報 | 請求方法・請求回数・請求予定日・請求対象費目・手数料・金額上書き（第4.5節参照） |
| 請求詳細 | 年度ごとの請求・入金の編集・履歴（旧・独立画面を統合） |

発電所一覧は**並び替え**（更新順 / 登録順 / 引渡日順 / 発電所名順）に対応し、選択はセッション内で保持する。各種項目のあらゆる値で横断検索できる。一覧は1行表示とし、長い項目は省略表示（全文はカーソルで確認）とする。

### 4.5 請求情報の設定

- **請求方法**：請求書 / 口座振替。口座振替の引落日は**25日固定**とする。
- **請求対象費目の選択**：保守内容の各金額費目（年次保守料・土地賃料・火災保険料・自治会費・通信費・その他）ごとに、請求計算へ含めるかを選択できる。含めない費目は記載のみで計算に入らない（例：自社委託でない費目）。
- **手数料**：請求書は発行手数料、口座振替は振替手数料を、税別／税込の自動換算付きで設定できる。
- **金額の個別上書き**：均等割の例外として、回・月ごとに金額を上書きできる（イレギュラー対応）。
- 編集画面には請求金額の**計算式**を段階表示し、各項目にカーソルで説明が出るインフォマークを配置する。

### 4.6 保守管理

- **保守対応**：問合日・発生日・対象箇所・状況・対応・報告を記録。状態（対応中 / 完了）で管理する。
- **定期保守**：点検・除草・経産省定期報告を記録。年内実施回数を表示。契約プランは点検・除草・駆けつけで「なし / 年1〜4回 / 無制限」から選択する。
- **受託 / 委託情報**：受託会社・保守委託先を一覧化。発電所名・顧客名・受託会社・保守委託先で検索できる。

### 4.7 請求管理

- **請求予定**：今月・来月・再来月に請求予定日が到来する回のうち、未発行のものを一覧表示し「発行」できる。
- **未入金 / 入金済**：対象は今年度＋昨年度。年複数回請求は**回単位**で判定し、回ごとに入金確認できる。
- **口座振替**：対象発電所を一覧し、振替失敗のマーク・取り消しができる。
- **請求・入金履歴**：発電所詳細の請求詳細タブで、過去年度の記録を回ごとに表示・追加・編集できる。

### 4.8 データ移行（インポート／エクスポート）

- **CSVインポート**：テンプレート形式、既存の各種レガシー形式に対応。UTF-8 / Shift-JISを自動判定する。
- **金額確認CSV**：1行＝1発電所、費目を横並びにした表を出力。発電所ごとの合計・費目ごとの合計を付す。
- **汎用エクスポート**：データ種別・項目を選択し、JSON（バックアップ／復元用）またはCSV（Excel用）で出力する。項目名で検索可。
- **復元**：JSONバックアップからデータを復元する。

---

## 5. 請求計算仕様

請求金額の算出は本システムの中核ロジックであり、単一の共通モジュールに集約し、請求画面・請求情報タブ・請求詳細・ダッシュボードのすべてで同一の計算を用いる。

### 5.1 年間総額

保守内容の各費目のうち「請求に含める」と設定された費目のみを合算した額を**年間総額（税込・請求対象のみ）**とする。含めない費目は記載のみで加算しない。

### 5.2 1回あたりの請求額

**請求書の場合**

```
年間総額（請求対象のみ）              A
÷ 請求回数 ＝ 1回あたり（均等割）     A ÷ n
＋ 発行手数料（税込・ありの場合）      F
─────────────────────────────
1回あたりの請求額                    A÷n ＋ F
```

**口座振替の場合**

```
年間総額（請求対象のみ）              A
÷ 12ヶ月 ＝ 月額（均等割）           A ÷ 12
＋ 振替手数料（税込・ありの場合）      F
─────────────────────────────
毎月の請求額                         A÷12 ＋ F
```

### 5.3 金額上書きと手数料の関係

> **【重要仕様】**
> 金額の個別上書きは「手数料を除いた基本額」を差し替える扱いとする。上書きした回・月にも手数料は自動加算されるため、上書き値には手数料を含めない。手数料込みの最終額をそのまま入力すると手数料が二重計上される。

### 5.4 入金判定

- 年複数回請求の記録は、回ごとに「請求日あり・入金日なし」を未入金と判定する。
- 全回の入金が揃って初めて、記録全体を「入金済」とする。
- 未入金・入金済の集計対象は今年度＋昨年度（過去1年）とする。

---

## 6. データ要件

データはSupabase（PostgreSQL）上の以下8テーブルで構成する。

| テーブル | 役割 | 主な関連 |
|---|---|---|
| customers | 顧客（個人 / 法人） | 1顧客 : 多発電所 |
| projects | 発電所（案件）。設備・住所・電力情報。 | `customer_id` |
| contracts | 契約。金額・請求方法・保守プラン・手数料・請求対象フラグ。 | `project_id` |
| annual_records | 年次の請求・入金記録。分割入金（回ごと）を保持。 | `contract_id` |
| maintenance_responses | 随時の保守対応記録。 | `project_id` |
| periodic_maintenance | 定期保守（点検 / 除草 / 経産省報告）。 | `project_id` |
| prospects | 見込み案件。成約で顧客へ変換。 | `converted_customer_id` |
| attachments | 添付ファイル（現状未使用）。 | `customer_id` |

> **【補足】**
> 請求記録（annual_records）の請求金額の内訳は `line_items`、分割入金は `payments` としてJSON形式で保持する。契約の請求予定日リスト・金額上書き・請求対象フラグも同様にJSON列で管理する。

---

## 7. 非機能要件

| 区分 | 要件 |
|---|---|
| 対応環境 | モダンブラウザ（PC / スマートフォン）。画面幅に応じてサイドバーはハンバーガーメニュー化し、一覧表は横スクロールに対応する。 |
| レスポンシブ | 狭い画面でも全機能を操作可能とする。表は折り返さず横スクロールで全項目を参照する。 |
| 性能 | 少人数運用・数百件規模のデータを前提とする（一覧取得は上限1000件。将来はページネーションを検討）。 |
| 可用性 / 配信 | Vercelによる静的配信。GitHub main への push で自動デプロイ。 |
| データ保全 | 全データのJSONエクスポートによりバックアップ・別環境への復元が可能。 |
| セキュリティ | Supabase Authによるログイン認証。接続情報は環境変数で管理する。 |
| 操作性 | 数値の税別／税込自動換算、全角→半角の自動変換、検索・並び替え状態の保持など、入力負荷を軽減する。 |

---

## 8. 制約・既知の留意事項

- 過去データの移行を進行中であり、年度ラベルの揺れ（前年分が翌年度として登録される等）が一部に残存しうる。請求集計は今年度＋昨年度を対象とすることで運用上の抜けを防いでいる。
- 同一入金の二重登録が過去に発生したため、請求記録には重複チェックの運用的配慮が必要である（発生分は是正済み）。
- データベーススキーマ定義（schema.sql）と実DBの間に、後付けの請求関連列（請求予定日リスト・金額上書き・請求対象フラグ 等）に関する差分が存在する。将来的に定義の整備が望ましい。
- 添付ファイル（attachments）はエクスポート対象外であり、現状は未使用である。
- 案件番号は現状未入力のため、案件番号による並び替えは番号入力後に有効化される。

> **【運用フロー】**
> 本システムはローカルで動作確認のうえ、クライアント環境（Vercel）へ順次デプロイして機能追加・改善を継続している。上記の留意事項は今後の改善候補であり、現行の業務運用に支障はない。

---

*ageful-CRM 要件定義書 ／ Version 1.0 ／ 本書は現行システムの実装に基づき作成*
